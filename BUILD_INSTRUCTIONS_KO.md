# 🔧 화면 테마 메뉴 빌드 가이드

## ⚠️ 중요: 최신 코드로 빌드하기

현재 GitHub의 최신 코드에는 **화면 테마 메뉴**가 포함되어 있습니다.
그러나 이전 코드로 빌드하시면 메뉴가 표시되지 않습니다.

---

## 📥 1단계: 최신 코드 받기

### 옵션 A: 기존 저장소 업데이트 (권장)
```bash
cd makecall
git fetch origin
git reset --hard origin/main
git clean -fd
```

### 옵션 B: 새로 클론
```bash
rm -rf makecall
git clone https://github.com/ringneck/makecall.git
cd makecall
```

---

## 🧹 2단계: 완전 초기화 (필수!)

```bash
# Flutter 캐시 완전 삭제
flutter clean

# Build 폴더 수동 삭제
rm -rf build/
rm -rf .dart_tool/

# 의존성 재설치
flutter pub get
```

---

## 📱 3단계: APK 빌드

```bash
# Release APK 빌드
flutter build apk --release

# 빌드 파일 위치 확인
ls -lh build/app/outputs/flutter-apk/app-release.apk
```

---

## ✅ 4단계: 최신 버전 확인

빌드 후 APK를 설치하고 다음을 확인하세요:

### 프로필 탭 메뉴 순서:
```
1. 기본 설정 (API, WebSocket 설정)
2. 🟡 화면 테마 ← 노란색 배경으로 표시됨!
   └─ 라이트 모드, 다크 모드, 시스템 설정
3. 활성 세션 관리
4. 내 단말번호
5. 알림 설정
6. 약관 및 정책
```

### 확인 방법:
- **"화면 테마"** 메뉴에 **노란색 배경**이 있어야 합니다
- 메뉴 위치: **기본 설정 바로 다음**
- 클릭 시: 라이트/다크/시스템 설정 다이얼로그 표시

---

## 🚨 문제 해결

### Q1: 여전히 화면 테마 메뉴가 안 보입니다
**A:** 다음 명령어로 완전 초기화 후 재빌드:
```bash
git reset --hard origin/main
flutter clean
rm -rf build/ .dart_tool/
flutter pub get
flutter build apk --release
```

### Q2: 빌드 오류 발생
**A:** Flutter 및 Dart 버전 확인:
```bash
flutter --version
# Flutter 3.x 이상 필요
```

### Q3: 이전 버전이 설치되어 있습니다
**A:** 앱을 완전히 삭제 후 새 APK 설치:
1. 기존 앱 제거
2. 새 APK 설치
3. 로그인 후 프로필 탭 확인

---

## 📋 최신 커밋 정보

```
Commit: c4355e5
Date: 2025-11-15
Message: "debug: Add highlighted styling to theme settings menu for visibility testing"

변경사항:
- 화면 테마 메뉴에 노란색 배경 추가
- 아이콘 및 텍스트 크기 증가
- 디버그 로그 추가
```

---

## 🔗 원격 저장소

- GitHub: https://github.com/ringneck/makecall
- Branch: main
- 최신 커밋: c4355e5

---

## 💡 TIP

빌드 전 항상 다음 명령어로 최신 상태 확인:
```bash
git log --oneline -1
# 출력: c4355e5 debug: Add highlighted styling...
```

이 커밋이 보이지 않으면 `git pull origin main` 실행!
